// Αναγνωριστικά 
START=SZ			// Προσδιορισμός αρχικής κατάστασης ως SZ
SZ:	a-z A-Z	_	-> S0	// Μετάβαση από την κατάσταση SZ στην:	S0 με inputs γράμματα και τον χαρακτήρα _
	* 		-> BAD  // 					BAD με input οποιονδήποτε άλλο χαρακτήρα
S0:	_ 0-9 a-z A-Z	-> S0	// Παραμονή την κατάσταση S0 με inputs _, αριθμητικό ψηφίο απο το 0 έως 9 και γράμματα κεφαλαία ή πεζά
	\n		-> GOOD	// Μετάβαση από την κατάσταση S0 στην:	GOOD με input newline \n
	*		-> BAD	// 				   	BAD με input οποιονδήποτε άλλο χαρακτήρα
GOOD(OK):			// Προσδιορισμός κατάστασης εξόδου (accepting state) ως GOOD 
